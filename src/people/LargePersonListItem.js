export const LargePersonListItems = ({ person }) => {
  const { name, age, hairColor, hobbies } = performance;
  return (
    <>
      <h3>{name}</h3>
      <p>Age: {age}</p>
      <h3>Hobbies:</h3>
      <ul></ul>
    </>
  );
};
